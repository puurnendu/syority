/**
 * M7.6C — Safety Module
 *
 * Barrel export for safety business logic.
 */

export { SafetyService } from './SafetyService';
export type { SafetyKpis, SafetyTrendPoint, SafetyLogSummary } from './SafetyService';
