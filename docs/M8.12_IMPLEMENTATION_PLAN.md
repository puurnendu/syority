# M8.12 — Implementation Plan

**Status**: Phase 0 — Awaiting Authorization  
**Date**: 2026-09-01

---

## Implementation Phases

### Phase 1: Schema & Migration
- Add `MaterialSupplyRecord` model to Prisma schema
- Add `MaterialConstraint` model to Prisma schema
- Extend `workpack_material_lines` with supply chain fields
- Add `material_constraints` relation to Activity model
- Generate and apply migration
- Verify tables created with correct columns, indexes, FKs
- **Gate**: Migration applied, `prisma generate` succeeds, zero regression

### Phase 2: Core Services
- `MaterialReadinessService`
  - `calculateReadiness(activityId, orgId)` — compute from material lines
  - `calculateEventReadiness(eventId, orgId)` — aggregate for event
  - `recalculateConstraints(eventId, orgId)` — batch recalculation
- `MaterialConstraintService`
  - CRUD for supply records
  - Constraint date calculation from supply data
  - Binding constraint identification
- `MaterialScheduleIntegrationService`
  - Pre-CPM constraint injection
  - Works with existing `calculateSchedule()` interface
- **Gate**: TypeScript clean, unit tests pass, services are pure/testable

### Phase 3: API Routes
- `/api/events/[eventId]/materials/constraints` — GET constraint summary
- `/api/events/[eventId]/materials/readiness` — GET readiness dashboard data
- `/api/events/[eventId]/materials/supply` — GET, POST supply records
- `/api/events/[eventId]/materials/supply/[id]` — PATCH, DELETE
- `/api/events/[eventId]/materials/recalculate` — POST trigger recalc
- All routes use `withTenantGuard` + `guardApi('nav.schedule')`
- **Gate**: All routes secured, TypeScript clean, API responses valid

### Phase 4: CPM Integration
- Modify schedule calculation call site to inject material constraints
- Modify ScenarioCalculationService to inject material constraints (read-only)
- Verify CPM results correctly reflect material delays
- Verify scenario calculations remain non-mutating
- **Gate**: CPM integration verified, no scheduleEngine.ts changes, scenario non-mutation confirmed

### Phase 5: Seed Data
- Create realistic material supply data for EVM Demo event
- Include: materials with various readiness states
- Include: supply records with different delivery statuses
- Include: activities with binding material constraints
- **Gate**: Seed data loads cleanly, data integrity verified

### Phase 6: Frontend
- `MaterialReadinessDashboard` component with:
  - Readiness KPI cards (ready/partial/not_ready/blocked counts)
  - Material constraint timeline
  - Activity-material dependency matrix
  - Supply record management table
- Add "Material Readiness" view tab to WorkspaceToolbar
- Integrate into planner workspace page
- **Gate**: Browser renders correctly, all tabs functional

### Phase 7: Regression & Certification
- TypeScript check: zero M8.12 errors
- Verify M8.7-M8.11 views unaffected
- Verify scheduleEngine.ts unchanged
- Verify protected services unchanged
- End-to-end test: material delay -> constraint -> CPM impact -> forecast
- Production build check
- **Gate**: Full certification

---

## Estimated Effort

| Phase | Complexity | Files |
|-------|-----------|-------|
| Phase 1: Schema | LOW | 2 files |
| Phase 2: Services | MEDIUM | 4 files |
| Phase 3: APIs | LOW | 5 files |
| Phase 4: CPM Integration | HIGH | 2 files (careful surgery) |
| Phase 5: Seed Data | LOW | 1 file |
| Phase 6: Frontend | MEDIUM | 3 files |
| Phase 7: Certification | LOW | audit only |
