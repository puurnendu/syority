# M8.4 — Workpack Model & Execution Schema Audit

This document records the comprehensive data model audit of Workpack execution capabilities, relationships, field structures, and lifecycle states in `prisma/schema.prisma`.

---

## 1. Workpack Entity Capabilities Audit

| Capability Area | Entity / Table Reference | Status | Field / Relational Mapping |
|---|---|---|---|
| **Workpack Master & Identity** | `model Workpack` (lines 72-166) | **EXISTS** | `id`, `workpack_number`, `workpack_id_code`, `title`, `status`, `event_id`, `site_id`, `unit_id`, `system_id`, `asset_id` |
| **Workpack Status & Lifecycle**| `enum WorkpackStatus` (lines 2845-2853)| **EXISTS** | `pending_ai_review`, `draft`, `under_review`, `approved`, `issued`, `closed`, `cancelled` |
| **Activity Execution & Progress**| `model Activity` (lines 9-70) | **EXISTS** | `planned_start`, `planned_end`, `actual_start`, `actual_end`, `progress_percent`, `status` (`not_started`, `in_progress`, `completed`, `on_hold`) |
| **Planned vs Actual Separation**| `model Activity` | **EXISTS** | Planned dates (`planned_start`, `planned_end`) and Actual dates (`actual_start`, `actual_end`) are strictly separate columns |
| **QA/QC Clearance & Hold Points**| `model qa_clearance_records` (lines 2342-2355) | **EXISTS** | `activity_id`, `workpack_id`, `cleared_by`, `cleared_at`, `certificate_number`, `witness_name`, `notes` |
| **Punch List Tracking** | `model PunchListItem` (lines 1133-1165) | **EXISTS** | `workpack_id`, `category` (A/B/C), `status` (`open`, `in_progress`, `closed`), `assigned_to`, `closed_at` |
| **Box-Up Clearance & JCC** | `model clearance_for_boxup`, `model job_completion_certificates` | **EXISTS** | `clearance_sign_offs`, `job_completion_certificates` (engineer, operations, QA signatures) |
| **Materials Execution** | `model WorkpackMaterial`, `model workpack_material_lines` | **EXISTS** | `quantity_required`, `quantity_issued`, `quantity_used`, `status` (`pending`, `reserved`, `issued`, `returned`) |
| **Tools & Rigging** | `model workpack_tools` (lines 2816-2836) | **EXISTS** | `category`, `tool_type`, `cert_required`, `status` |
| **Field Safety & Incidents** | `model SafetyLog`, `model SafetyIncident` | **EXISTS** | `event_id`, `log_date`, `shift`, `manhours_worked`, `ptw_issued`, `toolbox_talks`, `lti` |
| **Digital Documents** | `model WorkpackDocument` (lines 1579-1604) | **EXISTS** | `storage_path`, `mime_type`, `include_in_pdf`, `document_type` |
| **Workflow Audit Trail** | `model WorkflowTransition` (lines 1543-1559) | **EXISTS** | `from_status`, `to_status`, `action`, `comment`, `performed_by`, `performed_at` |

---

## 2. Conclusion & Schema Impact

*   **Zero Schema Changes Required:** All 12 required execution capabilities are 100% supported by the existing Prisma schema.
*   **Database Freezing:** The M7.9.4 baseline schema remains untouched.
