# M8.3.1 — Schedule Engine Logic & Calculation State Audit

This document records the code-level mathematical audit of `src/lib/scheduleEngine.ts` across all calculation states, relationship modes, float formulas, cycle handling, and boundary conditions.

---

## 1. Mathematical Calculation State Audit

| Calculation State | Implementation Code Reference | Mathematical Formula | Audit Verdict |
|---|---|---|---|
| **Early Start (ES)** | `lines 222-263` | $ES_j = \max(EF_{\text{pred}} + \text{lag})$ for FS | **PASS** |
| **Early Finish (EF)** | `line 261` | $EF_j = ES_j + D_j$ | **PASS** |
| **Late Finish (LF)** | `lines 290-327` | $LF_i = \min(LS_{\text{succ}} - \text{lag})$ for FS | **PASS** |
| **Late Start (LS)** | `line 326` | $LS_i = LF_i - D_i$ | **PASS** |
| **Total Float (TF)** | `line 341` | $\text{Total Float} = LS_i - ES_i$ (preserves negative float) | **PASS** |
| **Free Float (FF)** | `lines 344-353` | $\text{Free Float} = \min(ES_{\text{succ}} - \text{lag}) - EF_i$ | **PASS** |
| **Critical Path** | `lines 354-358` | $\text{is\_critical} = (\text{Total Float} \le \text{threshold})$ | **PASS** |
| **Finish-to-Start (FS)** | `line 247` & `line 310` | $ES_j \ge EF_i + L$, $LF_i \le LS_j - L$ | **PASS** |
| **Start-to-Start (SS)** | `line 249` & `line 312` | $ES_j \ge ES_i + L$, $LS_i \le LS_j - L$ | **PASS** |
| **Finish-to-Finish (FF)**| `line 251` & `line 314` | $EF_j \ge EF_i + L$, $LF_i \le LF_j - L$ | **PASS** |
| **Start-to-Finish (SF)**| `line 253` & `line 316` | $EF_j \ge ES_i + L$, $LS_i \le LF_j - L$ | **PASS** |
| **Positive Lag (+Nd)** | `lines 148-158` | Edge offset $L > 0$ added to predecessor dates | **PASS** |
| **Negative Lag / Lead** | `lines 148-158` | Edge offset $L < 0$ subtracted from dates | **PASS** |
| **Milestones ($0\text{d}$)** | `line 136` | $D=0 \implies ES=EF$ and $LS=LF$ | **PASS** |
| **Cycle Detection** | `lines 160-205` | Kahn's Algorithm $\implies$ returns `SCHEDULE_CYCLE_DETECTED` | **PASS** |
| **Target Project Finish**| `lines 292-299` | Sinks anchored to `target_finish_date` for negative float | **PASS** |
| **Disconnected Node** | `lines 224-234` | Starts at project reference date, float calculated to project end | **PASS** |
