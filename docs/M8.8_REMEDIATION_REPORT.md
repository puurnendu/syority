# M8.8 Remediation Report

## Overview

This report documents the systematic closure of all forensic findings from the M8.8 audit.

**Previous status**: 🔴 NOT READY (1 P0, 5 P1, 3 P2, 3 P3)
**Post-remediation**: 🟠 CONDITIONAL PASS (0 P0, 0 P1, 1 P2, 1 P3 remaining)

---

## Finding Closure Summary

| Finding | Severity | Status | Root Cause | Fix |
|---------|----------|--------|-----------|-----|
| F-001: Dashboard unreachable | **P0** | ✅ CLOSED | Never imported in page | Added import + render in `schedule` view |
| F-002: `float_erosion_hours` vs `float_erosion` | **P1** | ✅ CLOSED | UI interface wrong | Fixed interface + rendering |
| F-003: `forecast_end` vs `forecast_finish` | **P1** | ✅ CLOSED | UI interface wrong | Fixed interface + rendering |
| F-004: `variance_days` vs `variance_from_plan_days` | **P1** | ✅ CLOSED | UI interface wrong | Fixed interface + rendering |
| F-005: `method` vs `forecast_method` | **P1** | ✅ CLOSED | UI interface wrong | Fixed interface + rendering |
| F-006: `activities_count` vs `activity_count` | **P1** | ✅ CLOSED | UI interface wrong | Fixed interface |
| F-007: No `is_current` uniqueness constraint | **P2** | ✅ CLOSED | Missing DB constraint | Added partial unique index |
| F-008: Dead code `vd.variances` fallback | **P3** | ✅ CLOSED | Speculative fallback | Removed |
| F-009: Dead code `fd.forecasts` fallback | **P3** | ✅ CLOSED | Speculative fallback | Removed |
| I-04: No `proposed→review` transition | P2 | ⚠️ OPEN | Design gap — pre-existing | Non-blocking |
| H-06: CritPath over-fetches relationships | P3 | ⚠️ OPEN | Query scope — pre-existing | Non-blocking |

---

## Detailed Remediation

### Phase A: P0 — Dashboard Unreachable

**Root cause**: `ScheduleControlDashboard` was exported but never imported in the page.

**Fix**: Added import and rendered when `activeView === 'schedule'`, following the exact pattern used for `ResourcePlanningDashboard`.

**Files changed**:
- `app/(dashboard)/planner-workspace/page.tsx` — L28 (import), L302-303 (render)

### Phase B: P1 — Field Contract Mismatches (5 total)

**Root cause**: UI TypeScript interfaces had field names that didn't match service return types.

**Fix**: Updated all 5 mismatched fields in UI interfaces + rendering + added null safety.

**Files changed**:
- `src/components/planner-workspace/resources/ScheduleControlDashboard.tsx`

### Phase C: Baseline Uniqueness Constraint

**Root cause**: No DB-level uniqueness enforcement on `is_current` per event.

**Fix**: Created migration `20260831100000_m88_baseline_current_unique` with partial unique index + duplicate resolution.

**Verification**: ✅ Unique violation correctly raised on duplicate attempts.

### Phase D: Contract Testing — 96/96 ✅

Created `scratch/m88_contract_test.js` covering all 7 data domains + envelope + null safety + integration.

---

## Test Results

| Test Suite | Count | Result |
|-----------|-------|--------|
| Prisma validate | — | ✅ PASS |
| Next.js build | 377 pages | ✅ PASS |
| UI/API contracts | 96/96 | ✅ PASS |
| Baseline uniqueness | 3/3 | ✅ PASS |
| M8.7 regression | 5/5 untouched | ✅ PASS |
| Security | 8/8 | ✅ PASS |
| Browser acceptance | — | ⚠️ NOT VERIFIED |

**Browser note**: Dev server has database connection issue (infrastructure, not code defect).

---

## Remaining Findings (Non-Blocking)

- **I-04 (P2)**: No `proposed→review` transition — pre-existing design gap
- **H-06 (P3)**: CritPath over-fetches relationships by org — performance, not correctness

## Legacy Protection: ✅ ALL VERIFIED UNTOUCHED
