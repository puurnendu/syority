# M8.7 MASTER FORENSIC UI AUDIT

Generated: 2026-08-31

## Methodology

For every M8.7 feature, trace:
```
UI component → props → state/store → API request → API response → mapping → display
```

For mutations:
```
button → handler → API → service → database → revalidation → UI refresh
```

---

## Phase 2A — Planner Workspace UI

### HierarchyTreePanel.tsx

| Check | Status | Notes |
|-------|--------|-------|
| Props interface | ✅ | Receives `onSelect`, renders tree nodes |
| API endpoint | `/api/events/{eventId}/planner/hierarchy` | 🟡 STATIC_PASS |
| Empty state | ✅ | "No hierarchy data" message |
| Loading state | ✅ | Spinner |
| Error state | ✅ | Error message |

### WorkpackGrid.tsx

| Check | Status | Notes |
|-------|--------|-------|
| Data mapping | ✅ | Maps WorkpackGridRow correctly |
| Column definitions | ✅ | All fields used exist in service response |
| Inline editing | ✅ | Uses batch update API |
| Status display | ✅ | Color-coded badges |

### ActivityGrid.tsx

| Check | Status | Notes |
|-------|--------|-------|
| Data mapping | ✅ | Maps ActivityGridRow correctly |
| Field names match service | ✅ | `description`, `total_float`, `early_finish` |
| Predecessor display | ✅ | Formatted as "A0010FS" strings |
| UDF values | ✅ | keyed by UDF code |

---

## Phase 2D — Resource Planning UI

### ResourcePlanningDashboard.tsx

| Check | Status | Notes |
|-------|--------|-------|
| Event context | ✅ | Uses `useWorkspaceStore().selectedEventId` |
| API fetch chain | ✅ | Fetches 7 endpoints in parallel |
| Filter state | ✅ | Date range, resource type, contractor |
| Loading state | ✅ | Shows during parallel fetch |
| Error state | ✅ | Shows error message |
| No event state | ✅ | "Please select an Event" |

### API Fetch Chain Analysis

```
Dashboard → fetchData()
├── /api/events/{id}/resource-loading ✅ Works
├── /api/organizations/resource-types ✅ Works
├── /api/organizations/contractors ✅ Works
├── /api/events/{id}/shifts ✅ Works
├── /api/events/{id}/schedule/resource-kpis ❌ P0 (broken route)
├── /api/events/{id}/schedule/resource-forecast ❌ P0 (broken route)
└── /api/events/{id}/schedule/resource-risks ❌ P0 (broken route)
```

**P2-001**: KPI, forecast, and risk fetches fail silently. Dashboard shows empty intelligence panels. No error indication to user.

### ResourceCapacityEditor.tsx

| Check | Status | Notes |
|-------|--------|-------|
| CRUD operations | ✅ | Uses correct API endpoints |
| Upsert mutation | ✅ | POST to resource-capacity |
| Delete mutation | ✅ | DELETE to resource-capacity/[id] |
| Refresh on save | ✅ | Calls parent refresh callback |
| Validation | ✅ | Required fields checked |

### ShiftConfigurationPanel.tsx

| Check | Status | Notes |
|-------|--------|-------|
| CRUD operations | ✅ | Uses correct API endpoints |
| Create shift | ✅ | POST to shifts |
| Update shift | ✅ | PATCH to shifts/[id] |
| Delete shift | ✅ | DELETE to shifts/[id] |
| Form validation | ✅ | Start/end time validation |

### ResourceLoadingHistogram.tsx

| Check | Status | Notes |
|-------|--------|-------|
| Data consumption | ✅ | Receives `ResourceLoadingResult[]` |
| Chart rendering | ✅ | Stacked bar chart |
| Over-capacity line | ✅ | Shows capacity limit |

### ResourceHeatmap.tsx

| Check | Status | Notes |
|-------|--------|-------|
| Data source | API: `/schedule/resource-heatmap` | ❌ P0 (route broken) |
| Fallback | ✅ | Uses `loadingResults` from parent |
| Display | ✅ | Color gradient by utilization |

### ResourceKpiCards.tsx

| Check | Status | Notes |
|-------|--------|-------|
| Data source | Received as `kpis` prop | ✅ |
| Null handling | ✅ | Shows "—" when null |
| Card layout | ✅ | 4-card grid |
| Problem | ❌ | Always shows "—" because KPI API is broken |

### ResourceForecastChart.tsx

| Check | Status | Notes |
|-------|--------|-------|
| Data source | Received as `forecast` prop | ✅ |
| Null handling | ✅ | Shows nothing when null |
| Problem | ❌ | Never renders because forecast API is broken |

### ResourceRiskPanel.tsx

| Check | Status | Notes |
|-------|--------|-------|
| Data source | Received as `risks` prop | ✅ |
| Null handling | ✅ | Shows nothing when null |
| Problem | ❌ | Never renders because risk API is broken |

---

## Phase 2E — Leveling UI

### LevelingPreviewModal.tsx

| Check | Status | Notes |
|-------|--------|-------|
| Fetch simulation | ✅ | POST `/schedule/level-resources` |
| Display recommendations | ✅ | Table with columns |
| `activity_name` field | ⚠️ P1-007 | Displays `undefined` because service uses `act.name` |
| Apply button | ✅ | POST `/schedule/level-resources/apply` |
| Apply error handling | ✅ | Shows toast on error, handles 409 |
| Loading state | ✅ | Spinner with "Running simulation..." |
| Error state | ✅ | Red alert box |
| Empty state | ✅ | "No recommendations" message |
| Close behavior | ✅ | Resets state |

### Mutation Flow

```
Apply Button
→ POST /api/events/{id}/schedule/level-resources/apply
→ withTenantGuard (✅ correct import)
→ ResourceLevelingApplyService.applyRecommendations
→ ❌ CRASHES (P0-003/004: early_end, total_float_days)
```

**The Apply mutation will always fail at runtime.**

---

## Phase 2I — Intelligence UI

### UI State Handling Matrix

| Component | Loading | Empty | Error | Data | Status |
|-----------|---------|-------|-------|------|--------|
| ResourceKpiCards | ✅ | ✅ "—" | ❌ Silent | ❌ Never shown | P2-001 |
| ResourceForecastChart | ✅ | ✅ Empty | ❌ Silent | ❌ Never shown | P2-001 |
| ResourceRiskPanel | ✅ | ✅ Empty | ❌ Silent | ❌ Never shown | P2-001 |
| ResourceTrendPanel | ✅ | ✅ Empty | ❌ Silent | ❌ Depends on API | P2-001 |
| ResourceHeatmap | ✅ | ✅ Empty | ✅ Fallback | ⚠️ Uses fallback data | OK |

---

## Legacy UI

### ResourceHistogram.tsx

| Check | Status | Notes |
|-------|--------|-------|
| Modified by M8.7? | ❌ NOT MODIFIED | 17KB, untouched |
| API endpoint | `/api/schedule/resource-histogram` | ✅ Correct import/signature |
| Data flow | ✅ | Uses correct Activity fields |
| Regression | ✅ NO REGRESSION | |

### ScheduleContainer.tsx

| Check | Status | Notes |
|-------|--------|-------|
| Modified by M8.7? | ❌ NOT MODIFIED | 104KB, untouched |
| Impact | None | Does not use M8.7 services |

---

## UI Contract Mismatches

| UI Expects | Backend Returns | Location | Severity |
|------------|-----------------|----------|----------|
| `activity_name` (string) | `undefined` | LevelingPreviewModal | P1-007 |
| `kpis` (object) | `null` (API broken) | ResourceKpiCards | P2-001 |
| `forecast` (array) | `null` (API broken) | ResourceForecastChart | P2-001 |
| `risks` (array) | `null` (API broken) | ResourceRiskPanel | P2-001 |

---

## Summary

| Phase | UI Exists | Props Correct | API Works | Data Displays | Status |
|-------|-----------|--------------|-----------|--------------|--------|
| 2A | ✅ | ✅ | ✅ | ✅ | 🟡 STATIC_PASS |
| 2D (core) | ✅ | ✅ | ✅ | ✅ | 🟡 STATIC_PASS |
| 2D (intelligence) | ✅ | ✅ | ❌ | ❌ | ❌ FAIL |
| 2E (modal) | ✅ | ⚠️ | ✅ (sim) / ❌ (apply) | ⚠️ undefined names | ❌ PARTIAL |
| 2I | ✅ | ✅ | ❌ | ❌ | ❌ FAIL |
| Legacy | ✅ | ✅ | ✅ | ✅ | 🟡 STATIC_PASS |
