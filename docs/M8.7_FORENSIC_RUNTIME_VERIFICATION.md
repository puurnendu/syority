# M8.7 FORENSIC RUNTIME VERIFICATION

## Environment Constraints

| Capability | Status |
|-----------|--------|
| `npx prisma validate` | **BLOCKED** — sandbox access denied to Node.js directory |
| `npx prisma generate` | **BLOCKED** — sandbox access denied |
| `npm run build` | **BLOCKED** — sandbox access denied |
| `tsx` script execution | **BLOCKED** — sandbox access denied |
| Database connectivity | **UNKNOWN** — cannot verify |

---

## Verification Methodology

All findings in this audit were derived from **static source code analysis**.

No M8.7 code was executed at runtime during this audit.

This is consistent with the finding that **zero tests from any M8.7 phase were ever executed against a running database** — the test scripts exist but were never run.

---

## Runtime Predictions Based on Code Analysis

### If `npx prisma validate` were run:

**EXPECTED**: PASS
- The Prisma schema itself is valid
- Field naming is internally consistent within the schema
- The issue is service code referencing non-existent field names

### If `npm run build` were run:

**EXPECTED**: FAIL
- TypeScript compilation would likely succeed (field names in Prisma `select` are not statically checked at build time — Prisma generates runtime types)
- BUT: The `@/lib/auth/withTenantGuard` import would cause a build failure for 5 routes

### If any M8.7 API were hit at runtime:

| Route | Expected Result |
|-------|----------------|
| GET /api/events/[id]/schedule/constraints | **500** — Prisma error on `select: { name: true, total_float_days: true }` |
| POST /api/events/[id]/schedule/level-resources | **200** — Simulation MAY work (uses `include` not `select`; in-memory logic) |
| POST /api/events/[id]/schedule/level-resources/apply | **500** — Prisma error on `early_end`, `late_end`, `total_float_days` |
| GET /api/events/[id]/schedule/resource-kpis | **MODULE_NOT_FOUND** — wrong import path |
| GET /api/events/[id]/schedule/resource-forecast | **MODULE_NOT_FOUND** — wrong import path |
| GET /api/events/[id]/schedule/resource-risks | **MODULE_NOT_FOUND** — wrong import path |
| GET /api/events/[id]/schedule/resource-heatmap | **MODULE_NOT_FOUND** — wrong import path |
| POST /api/events/[id]/schedule/resource-scenarios/compare | **MODULE_NOT_FOUND** — wrong import path |

### Leveling Simulation (POST level-resources):

The simulation endpoint is the ONLY route that may partially work because:
- It uses `include: { resources: true }` (fetches all Activity fields, not a select subset)
- It uses `description` field name? **NO** — it accesses `act.name` via in-memory `simulatedActivities` map
- Wait — let me check: line 105 stores `name: act.name` but the Prisma Activity has `description`, not `name`
- With `include` (not `select`), Prisma would return all fields. `act.name` would be `undefined`.
- The simulation would produce recommendations with `activity_name: undefined`

**Revised prediction**: Simulation produces PARTIAL results (activity names are undefined, logic otherwise executes).

---

## Test Execution Classification

| Test File | Lines | DB Calls | Actually Executed? | Classification |
|-----------|-------|----------|-------------------|---------------|
| m87_phase2a_security_tests.ts | 127 | 0 | No | STRUCTURAL |
| m87_phase2b_security_tests.ts | 99 | 0 | No | STRUCTURAL |
| m87_phase2c_security_tests.ts | 186 | 0 | No | STRUCTURAL |
| m87_phase2c_verification.js | 83 | 0 | No | STRUCTURAL |
| m87_phase2d4_integration_tests.ts | 261 | Yes | No | NOT EXECUTED |
| m87_phase2e_leveling_tests.ts | 251 | Yes | No | NOT EXECUTED |
| m87_phase2e2_apply_tests.ts | 238 | Yes | No | NOT EXECUTED |
| m87_phase2f_e2e_tests.ts | 257 | Yes | No | NOT EXECUTED |
| m87_phase2g_tests.ts | 81 | Yes | No | NOT EXECUTED |
| m87_phase2h_data_integrity_tests.ts | 109 | Yes | No | NOT EXECUTED |
| m87_phase2h_leveling_regression_tests.ts | 64 | Yes | No | NOT EXECUTED |
| m87_phase2h_performance.ts | 41 | Yes | No | NOT EXECUTED |
| m87_phase2h_security_tests.ts | 60 | Yes | No | NOT EXECUTED |
| m87_phase2h_simulation_isolation_tests.ts | 61 | Yes | No | NOT EXECUTED |
| m87_phase2h_transaction_tests.ts | 49 | Yes | No | NOT EXECUTED |
| m87_phase2i_tests.ts | 139 | Yes | No | NOT EXECUTED |

**Summary**: 16 test files exist. Zero have been executed.
Many contain real Prisma calls that would fail due to the same field name mismatches found in production code.
