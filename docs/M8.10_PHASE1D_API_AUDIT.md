# M8.10 Phase 1D — API Audit

> **Date:** 2026-09-01
> **Status:** ✅ PASS

## Routes

| Route | Method | Security | Status |
|-------|--------|----------|:-:|
| `/api/events/[eventId]/schedule/evm/summary` | GET | withTenantGuard + guardApi('nav.schedule') | ✅ |
| `/api/events/[eventId]/schedule/evm/s-curve` | GET | withTenantGuard + guardApi('nav.schedule') | ✅ |
| `/api/events/[eventId]/schedule/evm/drill-down` | GET | withTenantGuard + guardApi('nav.schedule') | ✅ |
| `/api/events/[eventId]/schedule/evm/scenario-projection` | GET | withTenantGuard + guardApi('nav.schedule') | ✅ |

## Security Enforcement

| Check | Status |
|-------|:-:|
| Session authentication | ✅ All routes via `withTenantGuard` |
| Tenant isolation (organization_id) | ✅ `session.user.organization_id` used for all queries |
| Permission guard (nav.schedule) | ✅ All routes — per LD-10 / UD-2 |
| Event ownership validation | ✅ Via organization_id filtering in data layer |
| No raw Prisma objects exposed | ✅ Domain types returned |
| HTTP error codes | ✅ 404 for no baseline, 400 for missing params, 500 for server error |

## API Contracts

### GET /summary
```json
{ "success": true, "data": { "bac": 250000, "pv": 0, "ev": 162200, "ac": 169000, ... } }
```

### GET /s-curve
```json
{ "success": true, "data": { "dates": [...], "pv": [...], "ev": [...], "ac": [...] } }
```

### GET /drill-down
```json
{ "success": true, "data": { "event": {...}, "workpacks": [...] } }
```

### GET /scenario-projection
```json
{ "success": true, "data": { "scenarioId": "...", "liveEvm": {...}, "projectedEvm": {...}, "delta": {...} } }
```

## Convention Compliance

- Follows existing route structure under `app/api/events/[eventId]/schedule/`
- Uses same guard pattern as existing M8.8/M8.9 APIs
- No new authentication mechanisms introduced
