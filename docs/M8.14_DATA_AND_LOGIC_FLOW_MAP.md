# M8.14 — Data and Logic Flow Map

> **Module**: M8.14 | **Date**: 2026-09-01 | **Type**: READ-ONLY

---

## Master Data Flow

```mermaid
flowchart TD
    subgraph INPUT
        U1[Field User] -->|WhatsApp/Web| A1[Activity Progress Update]
        U2[Planner] -->|Web| A2[Schedule/Date Update]
        U3[Safety Officer] -->|Web| A3[Safety Log Entry]
        U4[Materials] -->|Web| A4[Supply Record Update]
        U5[Manager] -->|Web| A5[Scope Change Request]
    end

    subgraph SERVICES
        A1 --> S1[FieldExecutionService]
        S1 --> S2[ProgressCalculationService]
        S2 --> S3[SpiAdapter]
        A2 --> S4[scheduleEngine CPM]
        A5 --> S5[ScopeChangeApplicationService]
        S5 -->|creates activities| S2
    end

    subgraph DATABASE
        S1 --> D1[(Activity.progress_percent)]
        S1 --> D2[(Workpack.overall_progress cache)]
        A3 --> D3[(SafetyLog)]
        A4 --> D4[(MaterialSupplyRecord)]
        A5 --> D5[(ScopeChangeRequest)]
        S4 --> D6[(Activity.early_start/finish)]
    end

    subgraph CONSUMERS
        S2 --> C1[TA Dashboard]
        S2 --> C2[Portfolio Dashboard]
        S2 --> C3[Report Engine]
        S3 --> C1
        S3 --> C2
        D3 --> C4[Safety Dashboard]
        D4 --> C5[Materials Dashboard]
    end
```

## Logic Flow: Progress Calculation

```
Input: Activity[] (from Prisma findMany)
  │
  ├─ Filter: deleted_at IS NULL, status ≠ cancelled
  │
  ├─ For each activity:
  │   ├─ duration = activity.duration_hours ?? 0
  │   ├─ progress = clamp(activity.progress_percent ?? 0, 0, 100)
  │   └─ earned = duration × (progress / 100)
  │
  ├─ Aggregate:
  │   ├─ totalDuration = Σ(duration)
  │   ├─ completedDuration = Σ(earned)
  │   ├─ balanceDuration = totalDuration - completedDuration
  │   └─ weightedProgress = totalDuration > 0 ? round(completedDuration / totalDuration × 100) : 0
  │
  └─ Output: ProgressMetrics {
       weightedProgress, totalDurationHours, completedDurationHours,
       balanceDurationHours, totalActivities, completedActivities,
       inProgressActivities, notStartedActivities
     }
```

## Logic Flow: EVM Calculation

```
Input: Activity[] + BaselineActivity[] + Event
  │
  ├─ BCWS (PV):
  │   └─ Σ(baseline.budgeted_cost × time_proportion_to_status_date)
  │
  ├─ BCWP (EV):
  │   └─ Σ(activity.budgeted_cost × activity.progress_percent / 100)
  │
  ├─ ACWP (AC):
  │   └─ Σ(activity.actual_cost)
  │
  ├─ Derived:
  │   ├─ SPI = BCWP / BCWS
  │   ├─ CPI = BCWP / ACWP
  │   ├─ CV = BCWP - ACWP
  │   ├─ SV = BCWP - BCWS
  │   ├─ EAC = BAC / CPI
  │   ├─ ETC = EAC - ACWP
  │   └─ VAC = BAC - EAC
  │
  └─ Output: EvmSummary
```
