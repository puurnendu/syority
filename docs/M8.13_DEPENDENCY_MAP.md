# M8.13 Dependency Map

## Upstream Dependencies (M8.13 reads from)

```
                    Activity.progress_percent (INPUT)
                              ↓
                    ┌─────────┴──────────┐
                    ↓                    ↓
         Activity.duration_hours     Activity.status
                    ↓                    ↓
                    └─────────┬──────────┘
                              ↓
                     M8.13 Progress Service
                              ↓
                    ┌─────────┼──────────┐
                    ↓         ↓          ↓
               Workpack   Discipline  Contractor
              .event_id  .discipline_id .contractor_id
```

| Dependency | Model/Service | Read/Write | Risk |
|------------|--------------|-----------|------|
| Activity.progress_percent | Activity | READ | Low — stable field |
| Activity.duration_hours | Activity | READ | Low — stable field |
| Activity.status | Activity | READ | Low — stable field |
| Activity.planned_start/end | Activity | READ | Low — updated by CPM |
| Activity.actual_start/end | Activity | READ | Low — manual input |
| Workpack.event_id | Workpack | READ | None — FK |
| Workpack.discipline_id | Workpack | READ | None — FK |
| Workpack.contractor_id | Workpack | READ | None — FK |
| Workpack.unit_id | Workpack | READ | None — FK |
| Workpack.equipment_type | Workpack | READ | Low — free text |
| Asset.asset_type | Asset | READ | Low — free text |
| EquipmentType | EquipmentType | READ | None — master data |
| BaselineActivity | BaselineActivity | READ | None — frozen snapshot |
| ScheduleBaseline.is_current | ScheduleBaseline | READ | None — single flag |

## Downstream Consumers (services that should read from M8.13)

| Consumer | Currently Uses | Should Use |
|----------|---------------|-----------|
| Portfolio Dashboard (`portfolio-stats`) | Own inline calculation | M8.13 Progress API |
| Execution Cockpit (`FieldExecutionService`) | Own `calculateWeightedProgress()` | M8.13 Progress API (or share implementation) |
| Report ShutdownProviders | `Workpack.overall_progress` (cached) | M8.13 Progress API |
| Report ContractorProgress | `Workpack.overall_progress` (cached) | M8.13 Progress API |
| Report DisciplineProgress | `Workpack.overall_progress` (cached) | M8.13 Progress API |
| Report data-fetchers | Multiple inline calculations | M8.13 Progress API |
| WorkpackSummaryBar | `Workpack.overall_progress` (cached) | M8.13 Progress API |
| EVM Service (M8.10) | `Activity.progress_percent` directly | **NO CHANGE** — continues reading Activity directly |

## Cross-Module Dependencies

```
M8.7 Resource Intelligence ──→ Resource allocation data
         │
M8.8 Schedule/Change Control ──→ CPM calculation, dates
         │
M8.9 Scenario Planning ──→ Read-only scenario projections
         │
M8.10 EVM ──→ EV/PV/AC/SPI/CPI (separate cost-weighted calcs)
         │
M8.11 Scope Change ──→ New activities, progress denominator
         │
M8.12 Material ──→ Material constraints, readiness
         │
M8.13 Progress Intelligence ──→ READS from all above
                                 WRITES: progress aggregations
                                 DOES NOT: modify any upstream
```

## Schema Dependencies

| New Model | Depends On |
|-----------|-----------|
| `StandardActivityType` | `EquipmentType` (FK) |
| `Activity.standard_activity_type_id` (new field) | `StandardActivityType` |
| `Asset.equipment_type_id` (new field) | `EquipmentType` |

## Protected Systems (DO NOT MODIFY)

| System | Files | Reason |
|--------|-------|--------|
| CPM Engine | `src/lib/scheduleEngine.ts` | M8.8 certified |
| EVM Services | `src/core/evm/*` | M8.10 certified |
| Scenario Services | `src/core/schedule/scenario/*` | M8.9 certified |
| Resource Services | `src/core/resources/*` | M8.7/M8.8 certified |
| Scope Change Services | `src/core/scope-change/*` | M8.11 certified |
| Material Services | `src/core/materials/*` | M8.12 certified |
