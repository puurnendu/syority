# M8.5 — Execution Data Model Audit

This document records the comprehensive data model and service audit for **M8.5 — Field Execution Control Center (Daily Shift + Manpower + Actual Progress + Delays + Lookahead)**.

---

## 1. Relational Model & Schema Audit

Audit executed against `prisma/schema.prisma` across all operational execution dimensions:

| Capability Area | Entity / Table Reference | Classification | Field / Relational Mapping |
|---|---|---|---|
| **Shift Management & Reporting** | `model shift_reports` | **EXISTS** | `id`, `organization_id`, `site_id`, `unit_id`, `shift_type` (`day`/`night`), `shift_start`, `shift_end`, `report_text`, `report_summary`, `activities_completed`, `activities_overdue`, `activities_in_progress`, `open_constraints_count`, `critical_constraints` |
| **Shift Safety & Manpower** | `model SafetyLog` | **EXISTS** | `event_id`, `log_date`, `shift`, `manpower_planned`, `manpower_actual`, `manhours_worked`, `manhours_planned`, `cumulative_manhours`, `ptw_issued`, `ptw_closed`, `ptw_suspended`, `toolbox_talks`, `lti`, `near_miss`, `safety_notes` |
| **Activity Execution Actuals** | `model Activity` | **EXISTS** | `planned_start`, `planned_end`, `actual_start`, `actual_end`, `duration_hours`, `progress_percent`, `status` (`not_started`, `in_progress`, `completed`, `on_hold`, `cancelled`), `early_start`, `early_finish`, `late_start`, `late_finish`, `total_float`, `free_float`, `is_critical` |
| **Historical Progress Auditing** | `model ProgressLog` | **EXISTS** | `id`, `activity_id`, `organization_id`, `log_date`, `data_date`, `progress_percent`, `percent_complete`, `manhours_actual`, `logged_by`, `recorded_by`, `recorded_at`, `remarks`, `actual_cost` |
| **Manpower & Resource Allocation** | `model ActivityResource`, `model Resource`, `model Contractor`, `model Discipline` | **EXISTS** | `contractor_id`, `resource_type_id`, `planned_hours`, `actual_hours`, `headcount`, `crew_size`, `resource_name`, `assigned_date`, `notes` |
| **Delays & Constraint Register** | `model ConstraintLog` (`constraint_logs`) | **EXISTS** | `workpack_id`, `constraint_number`, `title`, `description`, `category` (`material`, `permit`, `scaffold`, `access`, `vendor`, `document`, `manpower`, `equipment`, `weather`, `inspection`, `other`), `severity` (`critical`, `high`, `medium`, `low`), `status` (`open`, `in_progress`, `resolved`, `deferred`, `cancelled`), `raised_by`, `raised_date`, `target_resolution`, `impact_on_schedule` |
| **QA/QC Clearance & Hold Points** | `model qa_clearance_records` | **EXISTS** | `activity_id`, `workpack_id`, `cleared_by`, `cleared_at`, `certificate_number`, `witness_name`, `notes` |
| **Punch Items & Categorization** | `model punch_items` | **EXISTS** | `workpack_id`, `category` (A/B/C), `status` (`Open`, `in_progress`, `closed`), `assigned_to`, `closed_at` |
| **Workflow State Transitions** | `model WorkflowTransition` | **EXISTS** | `from_status`, `to_status`, `action`, `comment`, `performed_by`, `performed_at` |
| **System Audit Logging** | `model SystemAuditLog` | **EXISTS** | `user_id`, `target_tenant_id`, `action`, `metadata` |

---

## 2. API & Route Inventory Audit

| Route Endpoint | Method | Capabilities Verified | Audit Classification |
|---|---|---|---|
| `/api/activities` | `GET`, `POST` | List and filter activities by `event_id` and `workpack_id` | **EXISTS / COMPLETE** |
| `/api/activities/[id]` | `PUT`, `DELETE` | Update `actual_start`, `actual_end`, `progress_percent`, `status`, `notes` without modifying planned dates | **EXISTS / COMPLETE** |
| `/api/activities/bulk` | `POST` | High-throughput batch creation, update, and deletion | **EXISTS / COMPLETE** |
| `/api/shift-reports` | `GET` | List shift reports by organization, site, and unit | **EXISTS / COMPLETE** |
| `/api/safety/logs` | `GET`, `POST` | Log daily safety manhours, shift headcount, PTWs, and incidents | **EXISTS / COMPLETE** |
| `/api/workpacks/[id]/constraints` | `GET`, `POST` | Retrieve and create workpack field constraints and delays | **EXISTS / COMPLETE** |
| `/api/workpacks/[id]/hold-points/[activityId]` | `PATCH` | Inspector clearance of mandatory QA hold points | **EXISTS / COMPLETE** |
| `/api/workpacks/[id]/workflow` | `POST` | Deterministic state transitions with QA hold-point gates | **EXISTS / COMPLETE** |

---

## 3. Schema Gap Analysis & Gate Conclusion

* **Schema Gap Result:** **ZERO GAPS**. All required entities, enums, relational connections, and field structures natively exist in `prisma/schema.prisma`.
* **Database Freezing:** The M7.9.4 baseline schema is 100% preserved with zero database migrations required.
