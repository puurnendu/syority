# M8.10 Phase 1 Gate

> **Status:** BLOCKED — Awaiting owner approval on Architecture Lock + 3 Unresolved Decisions.
> **Date:** 2026-09-01

---

## Pre-Conditions for Phase 1

| # | Condition | Status |
|---|-----------|--------|
| 1 | Phase 0 Discovery complete | ✅ Done |
| 2 | Architecture Lock document finalized | ✅ Done — [M8.10_ARCHITECTURE_LOCK.md](file:///c:/DEV/STO/docs/M8.10_ARCHITECTURE_LOCK.md) |
| 3 | EV Earning Rules defined | ✅ Done — [M8.10_EV_EARNING_RULES.md](file:///c:/DEV/STO/docs/M8.10_EV_EARNING_RULES.md) |
| 4 | Final Scope Decision reconciled | ✅ Done — [M8.10_FINAL_SCOPE_DECISION.md](file:///c:/DEV/STO/docs/M8.10_FINAL_SCOPE_DECISION.md) |
| 5 | EVM Formulas locked with zero-division rules | ✅ Done — [M8.10_ARCHITECTURE_LOCK.md §3](file:///c:/DEV/STO/docs/M8.10_ARCHITECTURE_LOCK.md) |
| 6 | Protected systems audited and listed | ✅ Done — 12 files frozen |
| 7 | Unresolved product decisions answered by owner | ⏳ **PENDING** |
| 8 | Owner explicit approval to begin Phase 1 | ⏳ **PENDING** |

---

## Unresolved Decisions (3 items — see [M8.10_FINAL_SCOPE_DECISION.md](file:///c:/DEV/STO/docs/M8.10_FINAL_SCOPE_DECISION.md))

1. **UD-1:** Resource Rate Tables — Defer (A) or Implement (B)?
2. **UD-2:** EVM Permission Granularity — Reuse `nav.schedule` (A) or New `nav.cost` (B)?
3. **UD-3:** AC Source of Truth — `Activity.actual_cost` (A) or `SUM(ProgressLog.actual_cost)` (B)?

---

## Phase 1 Work Breakdown (Authorized After Approval)

### Phase 1A: Schema & Migration
- Add `EvmSnapshot` model to Prisma schema.
- Run `prisma migrate dev` to create the table.
- **No changes** to existing Activity, BaselineActivity, Resource, or any M8.7/M8.8/M8.9 models.

### Phase 1B: Seed Data
- Create `prisma/seed-evm-demo.ts`.
- Generate 15–25 cost-loaded activities across 3–5 workpacks.
- Generate a ScheduleBaseline capturing `budgeted_cost` for all activities.
- Generate 7–10 days of ProgressLog entries with `actual_cost` and `progress_percent`.
- Generate at least 1 scenario with overrides for scenario EVM projection testing.

### Phase 1C: Core EVM Engine
- Create `src/core/evm/EvmCalculationService.ts` with all locked formulas.
- Create `src/core/evm/EvmSnapshotService.ts` for snapshot generation/persistence.
- Create `src/core/evm/EvmScenarioProjection.ts` for read-only scenario EVM.
- Write comprehensive unit tests for all EVM formulas including zero-division edge cases.

### Phase 1D: API Layer
- Create `app/api/events/[eventId]/evm/summary/route.ts` — returns current EVM KPIs.
- Create `app/api/events/[eventId]/evm/s-curve/route.ts` — returns time-phased S-curve data.
- Both routes use `withTenantGuard` + `guardApi('nav.schedule')`.

### Phase 1E: Dashboard UI
- Create `src/components/planner-workspace/evm/CostControlDashboard.tsx`.
- Implement S-Curve chart (PV vs EV vs AC using Recharts).
- Implement KPI cards (BAC, PV, EV, AC, CPI, SPI, CV, SV, EAC, ETC, VAC, TCPI).
- Implement WBS drill-down tree-grid.

### Phase 1F: Integration & Testing
- Runtime tests against seed data.
- Browser acceptance test (mandatory).
- Regression tests confirming M8.7/M8.8/M8.9 are unaffected.
- TypeScript compilation and production build verification.

---

## Estimated Phases

| Phase | Scope | Depends On |
|-------|-------|-----------|
| 1A | Schema + Migration | Owner approval |
| 1B | Seed Data | 1A |
| 1C | EVM Engine | 1A |
| 1D | API Layer | 1C |
| 1E | Dashboard UI | 1D |
| 1F | Testing & Certification | 1E |

---

## GATE STATUS

**BLOCKED.** Awaiting:
1. Resolution of UD-1, UD-2, UD-3.
2. Explicit owner approval: "Proceed with Phase 1."
