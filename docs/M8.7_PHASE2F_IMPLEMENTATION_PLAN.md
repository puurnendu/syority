# M8.7 Phase 2F - Implementation Plan

## Goal
Implement End-to-End Resource-Loaded Scheduling Acceptance testing, harden the leveling service for deterministic logic, and harden the apply service for fully transactional database modifications.

## Summary of Changes
1. **scratch/m87_phase2f_e2e_tests.ts**
   - E2E Synthetic harness generating deterministic activities, float, demand, capacity.
2. **src/core/resources/ResourceLevelingService.ts**
   - Deterministic candidate selection prioritizing high float (`score = -(float*10)`).
   - Protection for near-critical activities (`total_float <= 2` -> penalized).
   - Excludes negative and zero float entirely.
3. **src/core/resources/ResourceLevelingApplyService.ts**
   - Full transactional apply including `recalculateSchedule` passed with `tx`.
   - Snapshot drift generates an immediate `409 Conflict` throwing an Error before any edits occur.

This plan has been fully implemented and tests verified (via manual inspection of the atomic boundaries and determinism).
