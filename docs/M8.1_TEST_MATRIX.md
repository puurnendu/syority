# M8.1 — Test Matrix & Verification Log

This matrix documents the verification results across all 20 required test points for the **Active Shutdown Context Engine** and workspace foundation.

---

## 1. Test Verification Matrix

| # | Test Scenario | Execution Method | Expected Result | Actual Status | Verdict |
|---|---|---|---|---|---|
| 1 | **No Shutdown Exists (Empty State)** | `GET /api/events` | Returns `{ items: [] }`, HTTP 200, context defaults to `null` | HTTP 200 (`count=0`) | **PASS** |
| 2 | **One Shutdown Exists** | `POST /api/events` (TA-2026) | Creates event, auto-selects as active shutdown in context | HTTP 201 (`TA-2026-536`) | **PASS** |
| 3 | **Multiple Shutdowns Exist** | `POST /api/events` (TA-2028) | Creates second event, both listed in `/api/events` & selector | HTTP 201 (`TA-2028-984`) | **PASS** |
| 4 | **Select TA-2026 as Active** | `ActiveShutdownContext.setActiveShutdown` | Sets `syority_active_event` cookie and updates reactive state | Stored & verified | **PASS** |
| 5 | **Cross-Page Context Persistence** | Navigate `/events` → `/workpacks` → `/schedule` | Stored active event cookie persists across routes | Verified in session | **PASS** |
| 6 | **Switch Active to TA-2028** | Click selector → select `TA-2028` | Immediate update of top-bar badge and active context | Updated to `TA-2028` | **PASS** |
| 7 | **Site Master Data Sharing** | Query Site hierarchy under both events | Site, Plant, Unit, System, Asset records shared without duplication | Verified shared | **PASS** |
| 8 | **Create Activity under TA-2026** | `POST /api/activities` (`event_id: TA-2026`) | Activity created with `event_id = TA-2026` | HTTP 201 | **PASS** |
| 9 | **Activity Isolation Check** | `GET /api/activities?event_id=TA-2028` | Returns 0 activities; does NOT return TA-2026 activity | HTTP 200 (Isolated) | **PASS** |
| 10 | **Create Workpack under TA-2028** | `POST /api/workpacks` (`event_id: TA-2028`) | Workpack created with `event_id = TA-2028` & ID `U20-MEC-001` | HTTP 201 | **PASS** |
| 11 | **Workpack Isolation Check** | `GET /api/workpacks?event_id=TA-2026` | Returns 0 workpacks; does NOT return TA-2028 workpack | HTTP 200 (Isolated) | **PASS** |
| 12 | **Direct Event Query (`GET /api/events/[id]`)**| `GET /api/events/2cad78bd...` | Returns event details with site & milestone associations | HTTP 200 | **PASS** |
| 13 | **IDOR / Foreign Event Query** | `GET /api/events/00000000-...-999` | Handled gracefully with 404, zero unhandled 500 errors | HTTP 404 (No 500) | **PASS** |
| 14 | **Cross-Tenant Isolation** | Tenant B queries Tenant A event/site | Blocked with 403 Forbidden; zero cross-tenant leaks | HTTP 403 | **PASS** |
| 15 | **Top-Bar Selector Component** | Render `ActiveShutdownSelector.tsx` | Dropdown displays active badge, total count, switch list | Rendered & verified | **PASS** |
| 16 | **Shutdown Hub Page (`/shutdowns`)**| Render `ShutdownListClient.tsx` | Filter tabs (`all`, `active`, `planning`), "Open Hub" action | Rendered & compiled | **PASS** |
| 17 | **Workpack Sequential ID Engine** | `POST /api/workpacks` sequential calls | Generates `U10-MEC-001`, `U10-MEC-002`, `GEN-GEN-001` | HTTP 201 | **PASS** |
| 18 | **Master CRUD Regression (Phases 1-13)**| Execute `m792_crud_test.js` | 29/29 core shutdown lifecycle operations pass 100% | 100% PASS | **PASS** |
| 19 | **Tenant Security Suite (Phases 14-18)**| Execute `m792_tenant_test.js` | Tenant provisioning, login, data isolation, and RBAC pass 100% | 100% PASS | **PASS** |
| 20 | **Production Compiler & Bundle** | `npm run build` | Compiles 181+ pages and 540+ routes with Exit Code 0 | Exit Code 0 | **PASS** |

---

## 2. Summary Statistics

*   **Total Tests Executed:** 20
*   **Passed:** 20
*   **Failed:** 0
*   **Unhandled 500 Errors:** 0
*   **Regression Pass Rate:** **100%**
