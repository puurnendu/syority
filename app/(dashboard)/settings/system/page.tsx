import SystemSettingsClient from './SystemSettingsClient';

export const metadata = {
  title: 'System Settings | SYORITY',
};

export default function SystemSettingsPage() {
  return <SystemSettingsClient />;
}
