import IntegrationsClient from './IntegrationsClient';

export const metadata = {
  title: 'Integrations | SYORITY',
};

export default function IntegrationsPage() {
  return <IntegrationsClient />;
}
